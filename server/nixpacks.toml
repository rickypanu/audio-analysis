[phases.install]
cmds = [
  'python -m venv /app/.venv',
  './.venv/bin/pip install --upgrade pip setuptools wheel',
  './.venv/bin/pip install opencv-python',
  './.venv/bin/pip install insightface --no-deps',
  './.venv/bin/pip install -r requirements.txt'
]